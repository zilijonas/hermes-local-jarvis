{
  "name": "jarvis-voice-dashboard-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Jarvis voice assistant Hermes dashboard plugin frontend. Bundled to a single IIFE with esbuild; every visual element comes from window.HermesUI at runtime (see hermes-ui). React itself comes from window.__HERMES_PLUGIN_SDK__.React (not bundled).",
  "scripts": {
    "build": "./build.sh"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
