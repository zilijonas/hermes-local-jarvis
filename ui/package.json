{
  "name": "jarvis-voice-dashboard-ui",
  "version": "0.1.0",
  "private": true,
  "description": "Jarvis voice assistant Hermes dashboard plugin frontend. Bundled to a single IIFE with esbuild; React comes from window.__HERMES_PLUGIN_SDK__.React at runtime (not bundled).",
  "scripts": {
    "build": "./build.sh"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
